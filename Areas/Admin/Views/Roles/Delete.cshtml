@model VinhuniEvent.Models.Role

@{
    ViewData["Title"] = "Xoá quyền";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">

    <h2 class="fw-bold text-danger mb-3">Xác nhận xoá quyền</h2>

    <div class="alert alert-warning border-warning shadow-sm">
        <i class="bi bi-exclamation-triangle-fill"></i>
        Bạn có chắc chắn muốn xoá mục này không? Hành động này sẽ không thể hoàn tác.
    </div>

    <div class="card shadow-sm border-0 rounded-3">
        <div class="card-body">

            <h5 class="fw-semibold mb-3">Thông tin quyền</h5>

            <dl class="row mb-4">
                <dt class="col-sm-3 fw-semibold">Tên quyền:</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.RoleName)</dd>

                <dt class="col-sm-3 fw-semibold">Mô tả:</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Description)</dd>
            </dl>

            <form asp-action="Delete" class="d-flex gap-2">

                <input type="hidden" asp-for="RoleId" />

                <button type="submit" class="btn btn-danger px-4">
                    <i class="bi bi-trash-fill"></i> Xoá luôn
                </button>

                <a asp-action="Index" class="btn btn-secondary px-4">
                    <i class="bi bi-arrow-left"></i> Quay lại
                </a>

            </form>

        </div>
    </div>

</div>
