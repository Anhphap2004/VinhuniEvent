@model VinhuniEvent.Models.Event

@{
    ViewData["Title"] = "Xóa sự kiện";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="content-wrapper">
    <div class="page-header mb-4 d-flex align-items-center justify-content-between">
        <h4 class="fw-bold mb-0 text-danger">🗑️ Xóa sự kiện</h4>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left-circle me-1"></i> Quay lại
        </a>
    </div>

    ```
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-10">
            <div class="card shadow-sm border-0 rounded-3 p-4">
                <div class="alert alert-warning d-flex align-items-center" role="alert">
                    <i class="bi bi-exclamation-triangle-fill me-2 fs-5"></i>
                    <div>
                        Bạn có chắc chắn muốn <strong>xóa sự kiện này</strong> không?<br />
                        Hành động này không thể hoàn tác.
                    </div>
                </div>

                <dl class="row mb-0">
                    <dt class="col-sm-3 fw-semibold text-secondary">Tiêu đề</dt>
                    <dd class="col-sm-9">@Model.Title</dd>

                    <dt class="col-sm-3 fw-semibold text-secondary">Địa điểm</dt>
                    <dd class="col-sm-9">@Model.Location</dd>

                    <dt class="col-sm-3 fw-semibold text-secondary">Thời gian</dt>
                    <dd class="col-sm-9">
                        @Model.StartTime.ToString("dd/MM/yyyy HH:mm") - @Model.EndTime.ToString("dd/MM/yyyy HH:mm")
                    </dd>

                    <dt class="col-sm-3 fw-semibold text-secondary">Trạng thái</dt>
                    <dd class="col-sm-9">
                        @if (Model.IsActive)
                        {
                            <span class="badge bg-success">Hoạt động</span>
                        }
                        else
                        {
                            <span class="badge bg-secondary">Ngừng</span>
                        }
                    </dd>

                    <dt class="col-sm-3 fw-semibold text-secondary">Ảnh</dt>
                    <dd class="col-sm-9">
                        @if (!string.IsNullOrEmpty(Model.Image))
                        {
                            <img src="~/main/img/events/@Model.Image" alt="Event image"
                                 class="rounded border shadow-sm"
                                 style="max-width:200px; height:auto; object-fit:cover;" />
                        }
                        else
                        {
                            <span class="text-muted fst-italic">Chưa có ảnh</span>
                        }
                    </dd>
                </dl>

                <form asp-action="Delete" class="mt-4">
                    <input type="hidden" asp-for="EventId" />
                    <div class="d-flex justify-content-start gap-2">
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash3-fill me-1"></i> Xóa ngay
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-x-circle me-1"></i> Hủy
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    ```

</div>
